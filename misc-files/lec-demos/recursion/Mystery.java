public class Mystery {
    
    /* A mystery function named foo */
    public int foo(int n) {
        int res = 1;
        for(int i=1; i<n; i++)
            result = result * i;
        return result
    }

    /*
     * Q1. What is foo(0)?
     * Q2. What is foo(1)?
     * Q3. What is foo(2)?
     * Q4. What is foo(4)?
     *
     * Can anyone detect the pattern?
     * Given the pattern is there a different
     *   way of implementing this?
     * Is it better?
     */

}
